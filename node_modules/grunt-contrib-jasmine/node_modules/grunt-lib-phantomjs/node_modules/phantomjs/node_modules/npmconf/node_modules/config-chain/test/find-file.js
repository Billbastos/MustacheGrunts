
var fs = require('fs')
  , assert = require('assert')
  , objx = {
    rand: Math.random()
  }

fs.writeFileSync('/tmp/random-spec-config.json', JSON.stringify(objx))

var cc = require('../')
var path = cc.find('tmp/random-spec-config.json')

assert.equal(path, '/tmp/random-spec-config.json')